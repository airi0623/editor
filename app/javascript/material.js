function material() {
  const materialSelect = document.getElementById("material-select");
  //const materialMaterial = document.getElementById("material-auto");
  // alert("ページ読み込んだ")
  materialSelect.addEventListener("input",function(){
    const materialID = materialSelect.value
  
  });
};

window.addEventListener("load", material); 
